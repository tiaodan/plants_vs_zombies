#ifndef ZPEASHOOTER_H  // 创建完系统自带
#define ZPEASHOOTER_H  // 创建完系统自带

#include "zPlant.h"

// 豌豆射手类，继承zPlant
class zPeaShooter : zPlant  // 创建完系统自带
{  // 创建完系统自带
public:  // 创建完系统自带
    zPeaShooter();  // 创建完系统自带
    void act();  // 自己写，行为
};  // 创建完系统自带

#endif // ZPEASHOOTER_H  // 创建完系统自带
